# API Reference

This section is generated from the Python source using `mkdocstrings[python]`.

Use these pages when you need exact function signatures and module-level exports.

- `Python API`: consolidated module references for `honestroles` and submodules.

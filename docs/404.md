# Page Not Found

<script>
(function () {
  var path = window.location.pathname;

  // Support legacy deep links that include source markdown suffixes.
  if (path.endsWith(".md")) {
    var normalized = path.replace(/\/index\.md$/, "/").replace(/\.md$/, "/");
    window.location.replace(normalized + window.location.search + window.location.hash);
    return;
  }

  // Support direct index markdown links as well.
  if (path.endsWith("/index")) {
    window.location.replace(path + "/" + window.location.search + window.location.hash);
  }
})();
</script>

The page you requested does not exist at this URL.

Try one of these entry points:

- [Welcome](index.md)
- [Installation](start/installation.md)
- [Quickstart](start/quickstart.md)
- [Workflow Guides](guides/index.md)
